{#
/**
 * @file
 * Theme override to display Services as an unordered list.
 *
 */
#}
{% for item in items %}
    {% if loop.last %}
        {{ item.content }}
    {% else %}
        {{ item.content }},
    {% endif %}
{% endfor %}
