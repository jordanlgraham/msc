{#
/**
 * @file
 * Theme override to display Services as an unordered list.
 *
 */
#}
<ul{{ attributes }}>
    {% for item in items %}
        {{ item.content }},
        {% if loop.last %}
            {{ item.content }}
        {%  endif %}
    {% endfor %}
</ul>
